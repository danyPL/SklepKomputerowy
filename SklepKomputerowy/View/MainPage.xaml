<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SklepKomputerowy.ViewModel"
             x:Class="SklepKomputerowy.View.MainPage">


    <ContentPage.BindingContext>
        <local:MainViewModel/>
    </ContentPage.BindingContext>
    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <CollectionView ItemsSource="{Binding Computers}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <VerticalStackLayout>
                            <Image Source="{Binding ObrazekSrc}"/>
                            <Label HorizontalTextAlignment="Center" FontSize="Title" FontAttributes="Bold" Text="{Binding Nazwa}"/>
                            <VerticalStackLayout>
                                <Label Text="{Binding Kategoria, StringFormat='Kategoria {0}'}"/>
                                <Label Text="{Binding Cena, StringFormat='Cena {0}zł'}"/>
                                <Label Text="{Binding DostepnaIlosc, StringFormat='Dostepna ilosc w magazynie: {0}zł'}"/>
                                
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
